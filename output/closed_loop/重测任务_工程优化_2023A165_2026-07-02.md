# 重测任务_工程优化_2023A165_2026-07-02

## 1. 重测原因

本轮使用 `A165.pdf` 中的问题重述作为旧题输入，缺少独立官方题面、附件坐标数据和结果文件格式。A165 是优秀论文 PDF，包含解法与结果，存在答案泄漏风险。因此本轮虽然总控诊断得分为 88 且无 P3/P4/P5，但不能作为正式旧题通过计数。

## 2. 上轮失败标签

- P1-P10：无
- 附加材料风险：M1，旧题材料不独立，附件缺失

## 3. 本轮使用版本

- base：`prompt_base_v1.0`
- plugin：`plugin_optimization_v1`
- patch：`patch_A092_engineering_optimization`
- 参考 patch：`patch_A127_engineering_layout_optimization`

## 4. 本轮重点检查

1. 是否从官方赛题原文而不是优秀论文中读取题意；
2. 是否补齐定日镜坐标附件和结果文件格式；
3. 是否仍能先建立评价函数，再进入优化模型；
4. 是否不直接复用 A165 论文中的三分查找、蒙特卡洛或具体结果；
5. 是否能独立列出数据需求、图表计划和人工确认项。

## 5. 必须改善项

- 补齐官方题面 PDF 或文本；
- 补齐附件坐标数据；
- 补齐 `result2.xlsx` 和 `result3.xlsx` 输出格式要求；
- 只允许使用提示词、patch 和学习卡片中的通用经验，不允许读取优秀论文答案后再诊断；
- 如果继续使用 A165，必须把论文答案内容从测试输入中隔离。

## 6. 通过标准

1. 总控诊断得分 >= 80；
2. 不出现 P3/P4/P5；
3. 明确评价函数或目标函数；
4. 明确优化目标、决策变量、约束条件和评价指标；
5. 比较简单方法和智能优化算法；
6. 有数据需求表；
7. 有图表计划；
8. 有敏感性分析计划；
9. 有人工确认项；
10. 使用独立旧题材料，避免答案泄漏。

## 7. 如果仍失败

若补齐材料后仍低于 80 分，或出现 P3/P4/P5，则生成失败复盘卡，并优先判断问题来自 `plugin_optimization_v1` 还是 `patch_A092_engineering_optimization`。若只是材料仍不完整，则不修改提示词，继续补材料后重测。
