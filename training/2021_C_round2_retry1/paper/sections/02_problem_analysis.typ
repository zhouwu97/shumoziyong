#let locked = json("../../paper_source_lock.json").claims

= 问题分析

#import "../style.typ": paper-figure

#paper-figure(
  "figures/fig01_workflow.svg",
  [图 1 #h(0.5em) 四问技术路线与验证闭环。供应商评价形成候选信息，随后依次完成最小规模、原料结构和产能边界优化。],
)

== 问题一分析

供应商“重要性”不能等同于历史供货总量。若只按总量排序，长期被频繁下单的供应商会天然占优，也会把未订购周的零供货错误计为服务失败。因此，评价必须同时刻画规模、发生概率、波动和订单兑现四个维度，并明确零值的业务语义。

本文采用业务型线性加权评分，而不让样本离散度直接决定权重。能力反映生产保障上限，按单供货概率反映订单能否发生，稳定性刻画正供货状态下的波动，兑现率补充订单响应。评价权重具有业务主观性，排序只用于候选筛选，最终订购与运输决策仍由约束优化模型给出。等权条件下 S140 从第 #locked.s140_base_rank.display 名变为第 #locked.s140_equal_rank.display 名，正说明“重要性”必须与权重语义一起解释。

数据处理中还必须区分两类零值：#locked.not_ordered_zero_supply.display 个“未订购且零供货”记录不应惩罚供应商；#locked.ordered_zero_supply.display 个“已订购但零供货”记录则是真实供货失败。若混合处理，两者会同时扭曲供货概率、正供货均值和兑现率。

== 问题二分析

问题二存在天然的优先级。企业首先需要回答“至少需要多少家供应商”，在最少数量确定后才讨论经济订购，最后在采购成本不变的条件下降低运输损耗。若把三个目标直接加权相加，供应商数量、相对成本和损耗的量纲与数量级会干扰业务优先级，也不能保证先实现最少供应商。

因此采用词典序求解：先建立最小基数能力覆盖模型，得到供应商数量下界；再在正式运输、损耗和库存约束下证明该数量存在可行方案；随后固定供应商集合，依次最小化相对采购成本和运输损耗。能力最大的前 25 家在最有利损耗条件下仅提供 #locked.problem2_top25_net_capacity.display m³/周产品等价能力，仍低于需求；前 26 家的对应能力为 #locked.problem2_top26_net_capacity.display m³/周，且正式 MILP 可行，因而最少数量闭合为 #locked.problem2_minimum_supplier_count.display。

题目中的“尽量由一家转运商运输”在问题二中被落实为硬约束，使每个活跃供应商-周只对应一个承运商。该设置形成可直接执行的混合整数线性规划方案，但也增加二元变量规模。

#pagebreak()

== 问题三分析

问题三的目标不同于问题二：采购价格不再是首要目标，题意强调多用 A、少用 C、减少原料总量并降低运输损耗。由于生产 1 m³ 产品所需 A 类原料最少，在 C 类用量固定后，最小化总原料量会自然偏向 A 类。因此将目标依次设为“C 类最少、总原料最少、运输损耗最少”，并用顺序锁定最优值的方式实现词典序优化。

若对全部可用供应商强制配置单一承运商，需要引入大量二元变量。既有硬单承运商路线在 80 秒内未返回可行解，其状态只能记为#locked.problem3_hard_carrier_status.display，不能写成“不可行”。正式模型据此采用连续运输流，并将拆分次数与次要承运量作为软运营指标披露。

连续目标未设置供应商启用固定成本，因此出现 #locked.problem3_active_supplier_count.display 家正订购供应商并不违反已建约束，却会增加合同和管理复杂度。该结果必须作为模型边界保留，不能为了表面简洁而手工删除小额订购量；若要减少合作供应商，应另行加入启用变量和固定合作成本。

== 问题四分析

问题四不是把基准需求简单上调，而是在全部供应商能力、转运能力和损耗约束下，最大化每周损耗后可用于生产的产品等价到货量。A、B、C 三类原料先扣除运输损耗，再按单位产品耗量换算为统一产品等价量。

安全库存下界必须随产能同步变化。基准问题的两周安全库存为 #locked.initial_inventory.display m³产品等价量；当周产能变为决策变量后，初始库存与每周期末库存下界均应取该变量的两倍，不能继续固定为基准值。

最终 #locked.problem4_weekly_capacity.display m³/周是历史能力估计与平稳损耗参数下的模型预测值，较 #locked.base_weekly_production.display m³/周提高 #locked.problem4_increase_ratio.display。该数值用于刻画模型边界，不代表企业在未来波动、供应中断或合同变化下必然长期实现。
