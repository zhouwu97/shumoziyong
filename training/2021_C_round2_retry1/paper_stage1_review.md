# 2021-C 论文表现层重建：阶段一审查记录

## 1. 阶段范围

本阶段仅完成 Typst 工程初始化和前置章节迁移，包括摘要、问题重述、问题分析、模型假设与符号说明。`paper_full_draft.docx` 仅作为文字内容参考，未继续修改，也未复制其中的公式对象。

本阶段没有重新建模或重新求解，没有修改目标值、供应商选择、敏感性结论、验证结果及其解释边界。模型正文、求解结果、结果分析、参考文献与附录尚未继续迁移。

## 2. Typst 工程与模板参考边界

- 单一排版引擎：Typst 0.15.0。
- 主入口：`paper/main.typ`。
- 样式入口：`paper/style.typ`。
- 前置章节：`paper/sections/00_abstract.typ` 至 `paper/sections/04_symbols.typ`。
- 输出文件：`paper/output/阶段一.pdf`。
- MathModelAgent 参考文件：`tmp/MathModelAgent-reference/skills/5writing/templates/zh/cumcm/main.typ`。
- MathModelAgent 参考提交：`be9c59c1aaa13c3dcb74452ea5cae11dada27589`。

仅借鉴了 A4 纸张、四边 2.5 cm 页边距、正文和标题字体层级、标题编号层级、三线表线宽以及 Typst 原生图片/公式排版方式。未复制完整 Skill、工作流、Schema、Gate 或其他比赛模板。

当前页面统一为白底、黑色正文和黑色标题；没有彩色页眉、绿色标题、绿色表头或完整网格表。阶段一没有插入论文图片，因此不存在图内大标题保留问题。

## 3. 结果数字锁

`paper_source_lock.json` 已建立并由 `paper/main.typ` 读取。锁文件规定现有结果文件为唯一事实来源，禁止模型和结果变更，仅允许显示精度上的舍入。

| 检查项 | 结果 |
|---|---:|
| 锁定源文件 | 15 |
| 锁定关键 claim | 48 |
| 直接 JSON 指针 claim | 40 |
| 明示推导 claim | 8 |
| SHA-256 不匹配 | 0 |
| JSON 指针值不匹配 | 0 |

`problem3_hard_carrier_status` 按源文件中的完整说明锁定为“限时内状态未知”：硬单承运商路线在 80 秒内未返回可行解，因此不能记为“不可行”。该项使用明示推导而非把整段中文说明误作枚举值比较。

前 25 家与前 26 家净能力已按锁文件所述规则从 `results/baseline_result.json` 独立重算，分别为 28,044.40580420685 m³产品/周和 28,371.908756875317 m³产品/周；论文显示值仅做小数位截取，与锁定值一致。

## 4. 公式审查

阶段一迁移范围内共有 1 个块级编号公式。Typst 结构查询确认其为原生 `math.equation`，不是图片或 DOCX 公式对象。

| 编号 | 内容 | 可见性 | 变量定义 | 正文引用 |
|---|---|---|---|---|
| (1) | `E(v,k)=v/q_k` 及 `q_A=0.60, q_B=0.66, q_C=0.72` | 第 8 页完整可见 | `E`、`v`、`k`、`q_k` 及三类系数均在表 2 或紧邻正文定义 | 使用 Typst 标签 `eq-material-conversion` 引用，PDF 显示为“式 1” |

编号从 (1) 开始且连续，无重复编号、漏号或断开的正文引用。内联变量和表格中的数学符号不计作独立编号公式。

## 5. 章节与结构检查

- 一级编号章节 4 个：问题重述、问题分析、模型假设、符号说明。
- 二级编号章节 6 个：问题背景、需要解决的问题、问题一至问题四分析。
- 摘要置于正文编号体系之外，目录页码与实际页码一致。
- Typst 源文件中未发现 TODO、TBD、占位文本、图片调用或禁用的通用流程文本。

## 6. PDF 逐页检查

最终 PDF 共 8 页，已由 Poppler 重新渲染后逐页目视检查。

| 页码 | 内容 | 检查结果 |
|---:|---|---|
| 1 | 标题、摘要、关键词 | 标题层级清楚；正文无裁切；关键数字和单位可读 |
| 2 | 目录 | 点引线、章节层级和页码对齐正常 |
| 3 | 问题重述 | 枚举、缩进和段落间距正常 |
| 4 | 问题分析 2.1--2.2 | 数字、英文缩写和中文正文混排正常 |
| 5 | 问题分析 2.3--2.4 | 状态边界表述完整，无溢出或重叠 |
| 6 | 模型假设 | 三线表线型正确，A1--A9 行均完整可读 |
| 7 | 符号说明表前半 | 数学符号、上下标和单位清晰，表格无网格化 |
| 8 | 符号说明续表与公式 (1) | 续表、分式、公式编号和引用均完整可见 |

所有页面均为 A4 白底黑字，页码居中；未发现缺字、黑块、内容重叠、表格截断、页边界裁切或公式丢失。

## 7. 阶段结论

阶段一指定交付物已经齐备并通过结构、数字锁、公式和前 8 页视觉检查。按照任务边界，本次在此停止，不自动开始第二阶段，不 commit，不 push，等待人工审核。
