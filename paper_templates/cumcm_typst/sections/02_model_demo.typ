#import "../components.typ": three-line-table

= 二、公式与三线表示例

== 2.1 数学公式

设决策变量为 $x_i$，单位成本为 $c_i$，需求下界为 $D$。一个最小线性模型可写为

$ min_(x_i >= 0) sum_(i=1)^n c_i x_i $ <eq-demo-objective>

并满足约束

$ sum_(i=1)^n x_i >= D. $ <eq-demo-demand>

@eq-demo-objective 给出目标函数，@eq-demo-demand 给出需求约束。实际论文必须在公式首次出现处解释变量、参数和单位，并将数值结论绑定到可追溯的结果文件。

== 2.2 三线表

#three-line-table(
  [符号与含义示例],
  (1fr, 2fr, 1fr),
  ([符号], [含义], [单位]),
  (
    ($x_i$, [第 $i$ 类方案的决策量], [件]),
    ($c_i$, [第 $i$ 类方案的单位成本], [元/件]),
    ($D$, [总需求下界], [件]),
  ),
)

表格仅保留顶线、表头分隔线和底线，不设置表头底色。表后应紧接解释文字，说明表中变量如何进入模型，而不是连续堆放表格。
