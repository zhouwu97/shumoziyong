= 一、版式层级示例

== 1.1 二级标题

正文采用首行缩进和两端对齐。实际论文应使用连贯段落说明问题背景、建模思路和结果含义，不应把内部文件名、脚本路径或工作流状态写入正文。

=== 1.1.1 三级标题

三级标题用于组织局部推导或结果解释。标题字号与容器相匹配，不使用彩色标题、阴影、卡片式章节或装饰性页眉。

== 1.2 字体回退

中文字体按宋体候选、思源宋体候选和系统中文字体依次回退；英文和数学符号由可用字体补足。模板不提交字体文件，编译前应通过 `typst fonts` 检查目标机器上的实际字体。

== 1.3 段落组织

竞赛论文的段落应围绕一个明确论点展开。通常先说明建模对象或计算目的，再给出方法、公式或数据依据，最后解释结果对当前问题的意义。段落之间需要形成因果、递进或对照关系，不能把求解日志逐条搬入正文。

摘要、问题分析、模型建立和结果检验承担不同功能。摘要压缩方法与结论，问题分析解释子问题之间的逻辑，模型建立给出变量、目标和约束，结果检验则说明可行性、误差与敏感性。模板只提供层级和样式，不强制所有题目采用完全相同的章节数量。

== 1.4 黑白打印

正文和标题统一使用黑色。若实际数据图需要颜色，应同时使用线型、点型或纹理编码，使图表转换为灰度后仍能区分。表格不依赖底色强调重点，必要时可通过列顺序、加粗或文字说明突出关键结论。
