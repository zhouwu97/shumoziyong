#import "../components.typ": paper-figure

= 三、图与图注示例

图表应服务于明确论证。数据图必须来自真实结果；概念图必须明确标记其示意性质，不能冒充计算结果。

#paper-figure(
  block(width: 100%, inset: 1em, stroke: 0.8pt + black)[
    #grid(
      columns: (1fr, auto, 1fr, auto, 1fr),
      column-gutter: 0.8em,
      align: center + horizon,
      box(inset: 0.7em, stroke: 0.8pt + black)[结果数据],
      [→],
      box(inset: 0.7em, stroke: 0.8pt + black)[图表规划],
      [→],
      box(inset: 0.7em, stroke: 0.8pt + black)[论文论证],
    )
  ],
  [数据来源、图表规划与论文论证之间的关系示意],
)

该图是概念示意，只说明组件关系。正式论文中的图表规划还应记录源文件 SHA、目标章节、Claim ID、颜色规则和灰度可区分性。

== 3.1 验收说明

编译完成后，应将 PDF 逐页导出为 PNG，检查公式缺失、中文乱码、表格越界、图题重叠、页码异常和大面积空白。程序化检查只能发现部分问题，最终视觉结论必须由具备视觉能力的 AI 或人工逐页确认。

== 3.2 数据图与概念图

数据图的输入应来自冻结的结果文件或可复现绘图脚本，并记录源文件摘要。折线图适合表达时间趋势，条形图适合比较排名或方案差异，散点图适合呈现变量关系。选图时应先确定需要回答的问题，再决定图形类型，而不是先画图后寻找解释。

概念图用于描述流程、结构或变量关系。它不携带真实计算值，因此必须在图表规划中标记为概念图。图内节点文字应简短，详细定义和限制条件放入正文，避免把整段说明塞进图中造成字号过小。

== 3.3 图表前后文

正文在图表出现前说明观察目的，在图表之后解释主要模式、异常点及其对结论的影响。连续图表之间至少保留必要的过渡和分析，避免论文退化为没有论证链条的图集。
