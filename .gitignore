# 忽略原始 PDF 论文文件，避免把大体积参考资料上传到仓库。
*.pdf

# 忽略 PDF 抽取、渲染等临时中间产物。
tmp/

# 忽略 Python 脚本运行和语法检查产生的缓存。
__pycache__/
*.py[cod]
.coverage
htmlcov/

# 忽略自动下载的官方赛题包和附件原始文件，仅保留 README/manifest。
official_materials/**
!official_materials/
!official_materials/README.md
!official_materials/**/
!official_materials/**/manifest.md
!official_materials/**/material_manifest.json

# 忽略导出器生成的运行包与 manifest（CI 每次重新生成，避免提交非确定性时间戳）。
# 手工维护的注入提示词（mathmodelagent_inject_prompt.md 等）仍纳入版本管理。
export/cumcm_runtime_pack.md
export/cumcm_runtime_pack.manifest.json

# 工作流和比赛材料命令生成的 Run 目录不纳入版本控制。
runs/

# PR-8 隐藏题身份、配对映射、评审记录和人工签名只保存在私有资格工作区。
qualification_runs/

# MathModelAgent 上游只读 Source Asset。仅由受控同步器在本地生成，禁止提交。
.vendor/mathmodelagent/
.vendor/.mathmodelagent-stage-*/
.vendor/.mathmodelagent-backup/

