# 轻量提示词回归运行契约

本运行包仅用于轻量提示词行为回归。执行代理只能依据已编译的运行包和指定回归输入工作，不得将输出表述为完整旧题回放或比赛执行。

1. 本流程不生成 Gate 0-5 完整证据、不执行 complete 或 seal，也不产生 Patch 晋级资格。
2. 输出只用于检查提示词语义、边界和回归行为；不能替代真实材料、真实运行元数据或人工审核。
3. 任何候选 Patch 试验都必须明确标注为隔离回归，且不得进入正式比赛包。
